echo "🔍 Running Gitleaks before push..."
gitleaks detect --verbose

if [ $? -ne 0 ]; then
  echo ""
  echo "❌ Gitleaks found secrets in your repository!"
  echo "Please remove the secrets before pushing."
  echo ""
  exit 1
fi

echo "✅ No secrets detected. Proceeding with push..."
